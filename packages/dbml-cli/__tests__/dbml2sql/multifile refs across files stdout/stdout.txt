CREATE TABLE "users" (
  "id" int PRIMARY KEY,
  "email" varchar
);

CREATE TABLE "orders" (
  "id" int PRIMARY KEY,
  "user_id" int
);

ALTER TABLE "orders" ADD FOREIGN KEY ("user_id") REFERENCES "users" ("id") DEFERRABLE INITIALLY IMMEDIATE;

