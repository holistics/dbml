// base of the reuse chain: defines tables and an enum
Enum order_status {
  pending
  shipped
  delivered
}

Table users {
  id int [pk]
  name varchar
}

Table orders {
  id int [pk]
  user_id int
  status order_status
}

Ref: orders.user_id > users.id
