{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "./", /* Specify the root folder within your source files. */
    "paths": {
      "@/*": [
        "src/*"
      ]
    },
    "baseUrl": "."
  },
  "include": [
    "src",
    "__tests__"
  ]
}
